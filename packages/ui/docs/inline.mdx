# Inline
```jsx
import { Inline } from 'blockstack-ui'
```
Inline is also identical to `Box` but the `is` prop is set to `span`. [Read more about the `is` prop.](https://github.com/jxnblk/styled-system/tree/master/clean-tag#is-prop)

### Component Props


### System Props
