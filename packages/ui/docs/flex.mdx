# Flex
```jsx
import { Flex } from 'blockstack-ui'
```

This is identical to `Box` with the exception that the `display` style is set to `flex` by default. (to avoid having to always do `<Box display="flex" />`).

### Component Props


### System Props
