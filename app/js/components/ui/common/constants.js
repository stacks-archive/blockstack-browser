const BASE = 15
const GUTTER = BASE * 2
/* eslint-disable */
export const defaultAvatarImage = require('@images/avatar.png')
/* eslint-enable */

const base = `${BASE}px`
const gutter = `${GUTTER}px`

const spacing = {
  base,
  gutter
}

export { spacing }
