import React from 'react'

const KeyComplete = () => (
  <section>
    <h3>
      Key Complete
    </h3>
    <button onClick={() => {}}>Continue to Stealthy</button>
  </section>
)

export default KeyComplete
